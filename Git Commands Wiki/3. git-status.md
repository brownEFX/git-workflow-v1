# git log
- View the commit history of a repository

| ***Stage***                               | ***Key Commands***                                                   |
|-------------------------------------------|----------------------------------------------------------------------| 
| `git status'` + enter                     | 📌 View real-time summary of state of Git repository + WD and index. |
| `git status -s` + enter                   | 📌 View compact, two-letter status code format output.               |
| `git status -v` + enter                   | 📌 View additional detail, incl diffs of the uncommitted files.      |
| `git status -vv` + enter                  | 📌 View very verbose status information + full textual diffs.        |
| `git status --ignored` + enter            | 📌 View files explicitly ignored by .gitignore file.                 |
| `git status --porcelain` + enter          | 📌 View changes in WD and staging in machine-readable format.        |
| `git log --graph --oneline --all` + enter | 📌 View branch and merge history using an ASCII graph.               |
| `git log --grep="bug fix` + enter         | 📌 Searches for specific keywords in commit messages.                |
|                                           |                                                                      | 

