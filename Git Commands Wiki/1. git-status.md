# git status
- Do your updates in a branch, not main

| ***Stage***                                       | ***Key Commands***                                                               |
|---------------------------------------------------|----------------------------------------------------------------------------------| 
| `git status` + enter                              | 📌 Verify current state                                                          |
| `git status -s` or `git status --short` + enter   | 📌 View compact output: M(modified),A(added),D(deleted),R(renamed),??(untracked) |
| `git status -v` or `git status --verbose` + enter | 📌 View default status output + actual textual changes of uncommitted files      |
| `git status -b` or `git status --branch` + enter  | 📌 View branch information                                                       |
| `git status -u` or `git untracked-files` + enter  | 📌 View untracked files                                                          |
| `git status -M` or `git status --verbose` + enter | 📌 View && detect renames, optionally set similarity index                       |
