## 📊 Git Workflow Comparison

| Step                                      | **Branches – No Rebase**                                                                                                                                                                   | **Branches – Rebase**                                                                                                                                                                      |
|-------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1. Pre‑branch**                         | `git switch main` → `git pull origin main`                                                                                                                                                 | `git switch main` → `git pull origin main`                                                                                                                                                 |
| **2. Create & Switch**                    | `git switch -c <feature/feature-name>`                                                                                                                                                     | `git switch -c <feature/feature-name>`                                                                                                                                                     |
| **3. Develop (Add, Stage, Commit, Push)** | - Add Source Code <br> - `git add .` or `git add <file>` <br> - `git commit -m "message"` <br> - `git push -u origin <feature/feature-name>`                                               | - Add Source Code <br> - `git add .` or `git add <file>` <br> - `git commit -m "message"` <br> - `git push -u origin <feature/feature-name>`                                               |
| **4. PR**                                 | GitHubUI: Open PR → Review → Approve → Merge                                                                                                                                               | GitHubUI: Open PR → Review → Approve → Merge                                                                                                                                               |
| **5. Sync Local (after merge)**           | - Review First: `git fetch origin` → `git diff main origin/main` → `git merge origin/main` <br> - Quick Sync: `git pull origin main`                                                       | - Review First: `git fetch origin` → `git diff main origin/main` → `git merge origin/main` <br> - Quick Sync: `git pull origin main`                                                       |
| **6. Rebase**                             | *(Not applicable)*                                                                                                                                                                         | - `git switch <feature/feature-name>` <br> - `git fetch origin` <br> - `git rebase origin/main` <br> - Resolve conflicts → `git add <file>` → `git rebase --continue`                      |
| **7. Push after Rebase**                  | *(Not applicable)*                                                                                                                                                                         | - If branch was already pushed → `git push --force-with-lease origin <feature/feature-name>` <br> - If first push after rebase → `git push origin <feature/feature-name>`                  |
| **8. PR (second cycle)**                  | *(Already merged in Step 4)*                                                                                                                                                               | GitHubUI: Update PR (if open) → Review → Approve → Merge                                                                                                                                   |
| **9. Sync Local (final)**                 | `git switch main` → `git pull origin main`                                                                                                                                                 | `git switch main` → `git pull origin main`                                                                                                                                                 |
| **10. Cleanup**                           | - Delete remote: `git push origin --delete <feature/feature-name>` <br> - Delete local: `git branch -d <feature/feature-name>` (or `-D` if needed) <br> - Prune: `git remote prune origin` | - Delete remote: `git push origin --delete <feature/feature-name>` <br> - Delete local: `git branch -d <feature/feature-name>` (or `-D` if needed) <br> - Prune: `git remote prune origin` |

---

## 🎯 Key Differences
- **No Rebase**: Simpler, linear history. You merge `main` into your branch or just sync after PR merge.
- **Rebase**: Cleaner history, but requires conflict resolution and a force‑with‑lease push if the branch was already pushed.
- **PR Cycle**: In rebase workflows, you may need to update an existing PR after rebasing and pushing.
- **Risk**: Rebase rewrites history, so `--force-with-lease` is essential to avoid overwriting teammates’ work.

---